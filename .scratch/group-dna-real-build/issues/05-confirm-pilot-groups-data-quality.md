Type: task
Status: open

## Question

Get the identifiers of the 8 real groups chosen for the pilot from the user, and verify each one's Salesforce export row is clean: non-empty Group/Member/Companies Profile sections, and a Chair + Network Advisor value that can actually be mapped to a real user. Flag anything needing manual correction before import — the map's destination only requires these 8 groups to import cleanly, not the full 420-row export (see map's Out of scope).
