Type: prototype
Status: open

## Question

Design the AI-conversation PDF export for Admins. Spec §11 confirms the content scope (phone-chat-style bubbles, Chair/AI messages separated, timestamped, in time order; exportable per-field or for every conversation in one group) but not the visual specifics or where the export lives in the UI. Needed to actually analyze pilot outcomes, so this can't stay undesigned. Produce a concrete mockup: bubble layout/styling, and where the export button/action sits in the Admin console.
