Type: grilling
Status: open

## Question

Does this pilot need a separate staging/pilot Azure environment (resource group, SWA instance, Postgres server) before building on production resources, or should the real build go directly onto production infrastructure? My Path has no staging step, but the user is unsure whether that's the right call here too — needs a live decision, not a default.
